import type { RecipeGraph } from "../../brand-read/StudioBrandReadClient";

export function RecipesView({ graph }: { readonly graph: RecipeGraph }) { return <div className="domain-view recipes-view"><dl className="summary-grid"><div><dt>Recipe digest</dt><dd><code>{graph.recipeDigest}</code></dd></div><div><dt>Graph digest</dt><dd><code>{graph.graphDigest}</code></dd></div><div><dt>Affected targets</dt><dd>{graph.affectedTargetCount}</dd></div><div><dt>Ownership conflicts</dt><dd>{graph.ownershipConflicts}</dd></div></dl>{graph.nodes.length === 0 ? <p className="empty-state">No recipe nodes are available.</p> : <ol className="recipe-list">{graph.nodes.map((node) => <li key={node.recipeId}><h4><code>{node.recipeId}</code></h4><dl className="summary-grid"><div><dt>Source → target</dt><dd><code>{node.sourceAsset}</code> → <code>{node.targetAsset}</code></dd></div><div><dt>Depth</dt><dd>{node.depth}</dd></div><div><dt>Dependencies</dt><dd>{node.dependencies.join(", ") || "None"}</dd></div><div><dt>Dependents</dt><dd>{node.dependents.join(", ") || "None"}</dd></div><div><dt>Target state</dt><dd>{node.targetState}</dd></div><div><dt>Receipt</dt><dd><code>{node.receiptDigest ?? "None"}</code></dd></div></dl><p>Ordered operations:</p><ol>{node.operations.map((operation, index) => <li key={`${operation}-${index}`}>{operation}</li>)}</ol><p>Operation digest: <code>{node.operationDigest}</code></p></li>)}</ol>}</div>; }
