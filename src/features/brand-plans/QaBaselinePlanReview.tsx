import type { BrandQaBaselinePlanSummary } from "../../brand-plans/contracts";

export function QaBaselinePlanReview({ summary }: { readonly summary: BrandQaBaselinePlanSummary }) {
  const difference = summary.rasterDifference;
  return <section aria-labelledby="qa-plan-review"><h4 id="qa-plan-review">QA baseline plan details</h4><dl className="summary-grid"><div><dt>Profile / case</dt><dd><code>{summary.profileId}/{summary.caseId}</code></dd></div><div><dt>State</dt><dd>{summary.state}</dd></div><div><dt>Baseline identity</dt><dd><code>{summary.baselinePath}</code></dd></div><div><dt>Baseline digest</dt><dd><code>{summary.oldBaselineDigest ?? "none"}</code> → <code>{summary.newBaselineDigest}</code></dd></div><div><dt>QA digest</dt><dd><code>{summary.oldQaDigest}</code> → <code>{summary.newQaDigest}</code></dd></div><div><dt>Asset digest</dt><dd><code>{summary.assetDigests.old}</code> → <code>{summary.assetDigests.next}</code></dd></div><div><dt>SVG digest</dt><dd><code>{summary.svgDigests.old}</code> → <code>{summary.svgDigests.next}</code></dd></div><div><dt>Brand system</dt><dd><code>{summary.brandSystemDigests.old}</code> → <code>{summary.brandSystemDigests.next}</code></dd></div><div><dt>Renderer</dt><dd>{summary.renderer.id} {summary.renderer.version}; {summary.renderer.platformClaim}; <code>{summary.renderer.qualificationId}</code>; build <code>{summary.renderer.rendererBuildDigest}</code></dd></div><div><dt>Raster difference</dt><dd>{difference.changedPixels ?? "unavailable"} pixels; max delta {difference.maximumChannelDelta ?? "unavailable"}; bounds {difference.changedBounds ? `${difference.changedBounds.left},${difference.changedBounds.top}–${difference.changedBounds.right},${difference.changedBounds.bottom}` : "none"}; pixels <code>{difference.beforeDecodedPixelDigest ?? "none"}</code> → <code>{difference.afterDecodedPixelDigest}</code></dd></div></dl></section>;
}
