---
title: Structural Theme Preview
description: Fixed installed-package representatives for Theme Lab structure and code presentation.
---

## Prepared documentation view

This content is fixed. Theme Lab identifies the exact structural subset represented
by this view. [Inspect a Hero layout](/catalog/hero-centered).

```typescript title="preview.ts" {2} ins={3} del={4}
export const theme = "prepared fixture";
const marked = "fixed mark color";
const inserted = "fixed insertion color";
const deleted = "fixed deletion color";
```

Use the sidebar and pagination to navigate the prepared documentation. Hero copy,
sidebar group contents and syntax colors remain installed fixture data.
