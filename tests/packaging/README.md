# Packaging checks

The TFSB47H local smoke builds only an unsigned macOS-arm64 `.app`. Signing,
notarization, installer/update readiness, other desktop targets, and public
artifacts remain deferred to the coordinated release assignments.
